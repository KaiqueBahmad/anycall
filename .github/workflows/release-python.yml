name: Release Python Patch

on:
  push:
    tags:
      - 'python-v*'

jobs:
  release-python:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Extract version from tag
        id: version
        run: |
          TAG=${GITHUB_REF#refs/tags/}
          VERSION=${TAG#python-v}  # Remove 'python-v' prefix
          echo "version=$VERSION" >> $GITHUB_OUTPUT
          echo "Releasing Python patch $VERSION"

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip build twine

      - name: Sync package version with tag
        run: |
          cd implementations/python/lib
          sed -i -E "s/^version = \".*\"/version = \"${{ steps.version.outputs.version }}\"/" pyproject.toml
          grep -E "^version = " pyproject.toml

      - name: Build Python package
        run: |
          cd implementations/python/lib
          python -m build

      - name: Publish to PyPI
        run: |
          cd implementations/python/lib
          python -m twine upload dist/* -u __token__ -p ${{ secrets.PYPI_API_TOKEN }}
